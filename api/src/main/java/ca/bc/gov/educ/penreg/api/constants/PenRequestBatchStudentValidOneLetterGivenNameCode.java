package ca.bc.gov.educ.penreg.api.constants;

/**
 * The enum Pen request batch student valid one letter given name code.
 */
public enum PenRequestBatchStudentValidOneLetterGivenNameCode {
  /**
   * A pen request batch student valid one letter given name code.
   */
  A,
  /**
   * pen request batch student valid one letter given name code.
   */
  I,
  /**
   * O pen request batch student valid one letter given name code.
   */
  O,
  /**
   * Y pen request batch student valid one letter given name code.
   */
  Y
}
